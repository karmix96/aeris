# S8 — automated CFD for the AERIS flying wing: the whole story so far

**Written 2026-09-15, 21:00 (Athens time).** Covers S8 from its first day (3–4 September 2026) to
tonight. Written for a colleague who is new to CFD: every term is explained the first time
it appears, and the glossary (Appendix A) collects them. Every number was checked against the
committed reports, the run files or the chat record while writing. The checking found stale
or conflicting statements in the older documents, listed in §8.2. **Where this report and an
older document disagree, this report is the later reading.**

Companion documents, for depth: `AUDIT_2026-09-05.md` (the mesher), `AUDIT_2026-09-10.md` (the
CFD campaign), `05_s6_cfd_qualification/VV_ACCEPTANCE_2026-09-13.md` (acceptance criteria),
`STATE_2026-09-10.md` (operational state and traps), `PLAN_desktop_campaign.md` (the plan).

---

## Contents

0. [The one-page summary](#0-the-one-page-summary)
1. [Background: AERIS, CFD in twenty minutes, and why S8 exists](#1-background)
2. [How the S8 pipeline works](#2-how-the-s8-pipeline-works)
3. [Chronology: how we got here, day by day](#3-chronology)
4. [Study catalogue: every study, its question, its answer](#4-study-catalogue)
5. [The rulebook: metrics, criteria, ranges](#5-the-rulebook)
6. [Safeguards and fallbacks](#6-safeguards-and-fallbacks)
7. [Where we are now, and what is left](#7-where-we-are-now-and-what-is-left)
8. [Critical review: examined, challenged, cross-checked](#8-critical-review)
- [Appendix A — glossary](#appendix-a--glossary)
- [Appendix B — the defect ledger](#appendix-b--the-defect-ledger)
- [Appendix C — file map](#appendix-c--file-map)
- [Appendix D — the principal investigator's directions that shaped the work](#appendix-d--directions-that-shaped-the-work)

---

## 0. The one-page summary

**What AERIS needs.** AERIS designs a small blended-wing-body (BWB) drone: a flying wing, 1.5–2.5 m
tip to tip, cruising at 28 m/s at 1,500 m. The design space has 20 variables (chords, span,
sweeps, twist, dihedral, elevon). To explore it, the project needs aerodynamic forces for
many designs. The fast tools it already has (AVL, a vortex-lattice code, and NeuralFoil, a
learned airfoil model) are approximate. High-fidelity CFD is accurate but slow. The plan is
**multi-fidelity**: run CFD on a well-chosen set of designs, learn how the fast tools are
wrong, and correct them everywhere else.

**What S8 is.** S8 is the eighth strategy the project tried for turning a design into a
trustworthy CFD result without a human in the loop. It is a chain:

> design vector → wing surface (pyGeo loft) → **our own O-H structured mesh** → ADflow RANS
> solve (Spalart–Allmaras turbulence model) → convergence judgement → dataset row → audit

**What works, and is verified.**
- Meshing: **100 of 100 designs** mesh with zero folded cells, bit-identically on rebuild,
  with the wall exactly on the design surface (2.2e-16 m).
- The solver setup: friction drag within **0.3–0.6 %** of NASA's agreed answer (NACA 0012). On
  the one public case at our Reynolds number (NACA 4412) it matches NASA's reference code on
  friction, separation point and velocity profiles.
- The convergence rules: every equation is judged separately; iterative error sits hundreds of
  times below the effects being measured.
- The pilot: ten wings, four angles, at a coarse level (567k cells) and five wings at a
  medium level (1.1M cells) — 69 archived results.
- The multi-fidelity idea: AVL plus a learned offset predicts CFD lift on a wing it has not
  seen to rms 0.004, and the neutral point to 2.8 mm.

**What is not established — the honest part.**
- **Drag is not grid-converged.** Coarse → medium moves drag by −26 to −34 drag counts
  (13–16 %), almost all of it pressure drag. Halving or doubling the artificial dissipation
  moves coarse-mesh drag by −21 / +35 counts, and refining the trailing-edge spacing by about
  −10. The coarse mesh's drag is substantially a numerical artefact, and the fine levels that
  would show where it converges need the cloud.
- **The archive fails the rule we adopted on 13 September** (y+ ≤ 1 on every wall cell): the old
  tip cap reached y+ 4.2. All 69 rows must be re-run. The re-run is approved, but on hold until
  the settings are decided (below).
- **Model-form error is only bounded, not measured**: fully turbulent SA could overstate
  profile drag 1.4–2× if the real wing keeps laminar flow; no second turbulence model runs yet.
- **No experiment on an AERIS-like wing exists.** Validation is a chain of evidence, not a
  number.

**What happens next.** Tonight's queue tests the settings that move drag most: matrix
dissipation, whether the dissipation effect shrinks on a finer mesh, trailing-edge spacing on
the medium mesh, and a clean far-field test. Tomorrow the PI decides the settings. Then the
re-run (~17 h), then the cloud batch for the fine levels, then a real grid-convergence study
and the multi-fidelity model.

---

## 1. Background

### 1.1 AERIS and the operating point

| Quantity | Value | Source |
|---|---|---|
| Speed / altitude | 28 m/s at 1,500 m, ISA atmosphere | `mission_authority_v1.yaml` |
| Temperature, density, viscosity | 278.4 K, 1.0581 kg/m³, 1.7419e-5 Pa·s | same |
| Mach number | **0.0837** — essentially incompressible | same |
| Reynolds number | **1.53 million on a 0.9 m reference chord** (1.70 million per metre) | same |
| Angles of attack in the dataset | −2°, 0°, 4°, 8° | same |
| Sideslip, rates, controls | all zero | same |

The Reynolds number, `Re = ρVL/μ`, says how much inertia dominates viscosity. It decides the
boundary-layer thickness and whether the flow is laminar or turbulent. Each design's own
Reynolds number on its mean aerodynamic chord runs **0.78–1.21 million**; since 13 September
every dataset row records it.

### 1.2 CFD in twenty minutes

**The equations.** Air obeys the Navier–Stokes equations: conservation of mass, momentum (three
components) and energy. Solving them directly for turbulent flow at Re 1.5 million is far too
expensive, so engineering CFD uses **RANS** (Reynolds-Averaged Navier–Stokes). It solves for the
time-averaged flow and represents the effect of turbulence with a **turbulence model**. S8 uses
**Spalart–Allmaras (SA)**, a one-equation model built for wings and the most-used model in
external aerodynamics. So every S8 solve has six coupled equations: continuity, three momentum,
energy, and SA's turbulence variable ν̃ ("nu-tilde").

**The mesh.** The equations are solved on a **mesh** (or grid): the space around the wing is
cut into small cells, and the solver balances the flow through each one. Two kinds matter:
- *Structured* meshes are logically rectangular blocks, indexed (i, j, k). They are efficient
  and accurate, but hard to generate automatically around a complex shape.
- *Unstructured* meshes use arbitrary cells. They are easy to generate and less efficient.

S8 is structured.

**The boundary layer and y+.** Next to the wing surface the velocity drops to zero in a thin
**boundary layer**, a few millimetres here. Friction drag is decided inside it. To resolve it
without shortcuts ("wall functions"), the first cell must sit in the *viscous sublayer*:
**y+ ≤ 1**. y+ is the wall distance scaled by the friction velocity and viscosity. Above about 5
a cell sits in the *buffer layer*, where the turbulence model's wall behaviour is not
calibrated. S8 is a **wall-resolved** mesh: y+ ≤ 1 on every wall cell is mandatory (PI
decision, 13 September).

**Drag, and its two parts.** Force coefficients are forces divided by dynamic pressure and a
reference area: CL (lift), CD (drag), CMy (pitching moment). Drag splits into
- **pressure drag CDp** — pressure pushing on the surface; includes induced drag from the tip
  vortices and form drag from the boundary layer's displacement;
- **friction drag CDv** — shear stress along the surface.

A **drag count** is 0.0001 in CD. Our wings' CD is about 180–210 counts, so one count is about
0.5 %. International drag-prediction workshops find codes scatter by 4–5 counts on the same
case.

**Convergence: are the equations solved?** The solver iterates. After each iteration the
**residual** measures how far the discrete equations are from balanced; convergence means the
residuals have fallen by many orders of magnitude *and* the forces have stopped changing. The
remaining **iterative error** must be far smaller than the effects being measured.

**Three kinds of error.** Every CFD number carries
1. **iterative error** — the equations were not fully solved (controlled by convergence rules);
2. **discretization error** — the mesh is too coarse to represent the flow (controlled by
   refining the mesh);
3. **model error** — RANS and SA are approximations of real turbulence, and transition from
   laminar to turbulent flow is not modelled at all (controlled only by comparing with
   experiment or other models).

**Grid convergence (GCI).** To measure discretization error, solve the same case on a
*family* of systematically refined meshes: same topology, every cell spacing divided by a
**refinement ratio r**, here r = 1.3. With three levels, **Richardson extrapolation** estimates
the answer on an infinitely fine mesh and the **observed order p** at which the error shrinks.
The **Grid Convergence Index** (Celik et al. 2008) turns that into an uncertainty band. With
two levels you only get a *trend*, not a band. S8 has reached two levels on the desktop; the
third and fourth need the cloud.

**Verification and validation.** *Verification*: are we solving the equations right? Compare
with known answers — other codes, analytic solutions, grid families. *Validation*: are they the
right equations? Compare with experiment. They are different questions, and a code can pass the
first and fail the second.

**Numerical (artificial) dissipation.** ADflow's flux scheme is *central* differencing, which
on its own is unstable. So it adds a small artificial smoothing term, controlled by
coefficients `vis2` and `vis4` with "scalar dissipation" by default. That smoothing acts like
extra viscosity. On a coarse mesh it can create spurious pressure drag, and it vanishes only
as the mesh is refined. This became central on 15 September (§4, G5).

### 1.3 Why automated meshing of a BWB is hard, and what came before S8

The project runs a governed "tournament" of meshing strategies (`ADR-0011`): each built
independently, judged on frozen metrics, a hold-out set of ten designs locked until the end.

| Strategy | Idea | Why it was not the answer |
|---|---|---|
| S0–S5 | tip-first marching, cross-field layouts, station sweeps, multiblock, frozen RBF | the Stage-02 versions shared one tip closure and "compared one topology wearing four labels"; rebuilt independently under ADR-0011 |
| S2 | cross-field quad layout | excellent on one geometry; 27/31/37/39 blocks across six designs — **not deterministic** |
| S6 / S7 | the "C family": six blocks per section with leading- and trailing-edge collars, pyHyp hyperbolic marching; S7 added an unstructured Gmsh/SU2 route | reached CFD, but the nose collar starved leading-edge resolution: **25.7–37.9° of surface turning per cell** against a conventional 10°; surface cp rose to **5.3** on candidate C03, physically impossible (bound ~1.002); that strip carried ~80 % of pressure drag. Found in all 65 surface meshes of the study |
| "D family" | fix the collar inside that topology | reached 10.6° per cell, then pyHyp **segmentation-faulted 25 times** — the collar size also set the trailing-edge and tip-cap point counts |

On 4 September the PI directed: *"i want to debug why the cfd doesnt converge. is it because of
the mesh or the solver? propably the mesh. so we shoudld change our meshing approach"* and
*"i want to think like an anegineer and make it work with y+<1"*. **S8 was the answer**: an
O-H structured grid after Zhang et al. (2026), with no collars, meshed by an algebraic march
that cannot crash (`ADR-0018`).

---

## 2. How the S8 pipeline works

### 2.1 Geometry: from 20 numbers to a wing surface

**The design vector** (`configs/geometry/bwb.yaml`). Semi-span means root to one tip.

| Group | Variable | Range | Meaning |
|---|---|---|---|
| Planform | `c1_m` | 0.70–1.10 m | root chord |
| | `c2_ratio`, `c3_ratio`, `c4_ratio` | 0.55–0.80, 0.30–0.55, 0.08–0.20 | chords at span breaks b1, b2 and the tip, over root chord |
| | `b_total_m` | 0.75–1.25 m | **semi-span** |
| | `b3_ratio` | 0.40–0.55 | outer-panel span fraction |
| | `split_ratio` | 0.40–0.60 | inner/outer leading-edge spline split |
| | `sw1_deg`, `sw2_deg`, `sw3_deg` | 20–40°, 15–35°, 5–25° | leading-edge sweep, inner / mid / outer |
| Twist | `twist_b0..b3_deg` | −1…1°, −3…0°, −5…−1°, −8…−2° | incidence at root and span breaks; negative outboard is **washout** |
| Dihedral | `dihedral_b1..b3_deg` | 0 (pinned), 0–6°, 0–10° | root panel held flat |
| Elevon | `elevon_start/end/hinge_frac` | 0.50–0.70, 0.85–0.98, 0.65–0.82 | control surface (undeflected in all CFD) |

Airfoils are fixed per span station: **MH91** at root and first break, **E374** at the second,
**NLF1015** at the tip, blended in between. Twist and dihedral vary piecewise-linearly between
the four stations.

**Design sets.** `lhs100_seed42`: 100 designs by Latin-hypercube sampling, for development.
`round_c_lhs10_seed42`: **the hold-out, never touched**. The **ten pilot geometries**
(12, 13, 16, 23, 29, 36, 47, 65, 81, 83) were chosen by maximin over the normalised design
variables, seeded with the reference design 83 and former folding designs. Their coverage
distance is 1.69, against a random-ten median of 1.78. Design **83** is the reference design
behind most single-geometry studies.

**The loft.** The generator builds 17 section records; pyGeo lofts a smooth B-spline surface
through them. The CFD surface opens the sharp trailing edge symmetrically to a blunt base of
0.5 % of chord (1 mm minimum), which structured meshing needs. The low-fidelity AVL model is
written from sections *extracted* from the same loft, so AVL and CFD fly the same wing. On 15
September AVL's sections were measured within 0.03–0.13 % of chord of the loft.

### 2.2 The mesh: O-H topology, explained

Picture slicing the wing at one spanwise station: you see an airfoil. Around it S8 builds an
**O-grid**: one closed ring of points hugging the airfoil (trailing edge → upper surface →
leading edge → lower surface → trailing edge), then rings marching outwards like tree-trunk
growth rings, to a circle 40 root chords away. Stack those O-slices along the span and you have
the **H** direction: root symmetry plane → tip → beyond the tip. "O-H" = O around the section,
H along the span.

**Three volume blocks** (sizes are cells at the coarse level `gci_C`):

| Block | What | Cells |
|---|---|---|
| `o_wing` | the O-slices from root to tip, 92 around × 64 outwards × 48 spanwise | 282,624 |
| `o_out` | the same O-slices continued beyond the tip to the spanwise far field | 276,736 |
| `cap_out` | a thin H-block closing the space in front of the flat tip face | 7,896 |

**Two surface blocks** make the whole wing wall: the wing skin, and the **tip cap**. The tip cap
is a single H-patch whose outline *is* the airfoil-shaped tip section, node for node. The C
family needed thirteen surface blocks. Block faces meet node-to-node (14 connections, none
open), and the O-ring's first and last points are shared, so the ring closes without a seam
gap.

**How a mesh is built** (`build_volume.py`, `march_o.py`, `strategy_s8.py`):
1. **Section ring.** Points around each section are spaced by two-sided hyperbolic (Vinokur)
   stretching and re-evaluated on the exact loft. Leading-edge spacing is *solved for*: the ring
   is refined until each cell turns the surface by at most **10°**. Trailing-edge spacing is
   `ds_te_frac` = 0.4 % of chord.
2. **Spanwise smoothing.** Leading-edge spacing may grow by at most 1.15× between neighbouring
   stations (defect 15 had a 2.6× jump).
3. **Span law.** Spanwise spacing clusters at the tip, so the tip cap's first cell is
   `tip_span_first_cell_in_s0` × s0 (2 since 13 September; it was 10).
4. **Wall-normal march.** An algebraic march in the section plane blends from the wall normal to
   a radial direction, with Laplacian smoothing that grows away from the wall. pyHyp was
   deliberately not used: it crashed 25 times on the D family. An algebraic march cannot crash,
   and every cell can be checked afterwards. First-cell height s0 is `s0_frac` 3.6e-6 of the
   bounding-box diagonal (4.73e-6 m on design 83); 65 layers; far field 40 root chords.
5. **Marching frame.** Each section marches in a plane square to the span (`span_normal`). An
   earlier best-fit (SVD) plane tilted up to 8°, and 40 m out that tilt folded cells on 15 % of
   designs (defect 20). Each node keeps its own out-of-plane offset, so the wall stays exactly
   on the loft (defect 21).
6. **Validity.** Every assembled hexahedron's volume is checked. If any is negative, the build
   climbs a smoothing ladder; if the ladder is exhausted, **the build fails** rather than writing
   a bad grid (defect 19).
7. **Guards.** The wall layer must equal the loft to 1e-9 m. The tip-cap cell must not exceed
   3× s0. The mesh is written to CGNS for ADflow.

Meshing costs about **12 seconds per design**. Solving costs hours. So mesher speed is never the
bottleneck.

**The grid family** (one refinement ratio, `refined_level`, r = 1.3 in each direction inside
`o_wing`):

| Level | Cells | Where it runs | Memory (ANK-only law) |
|---|---|---|---|
| `gci_C` (coarse, = `oh_L3`) | 567,256 | desktop | ~6.8 GiB |
| `gci_M` (medium) | 1,111,152 | desktop, just | ~10.7 GiB |
| `gci_F` (fine) | 2,217,680 | cloud only | ~19.5 GiB |
| `gci_FF` (extra-fine) | **4,504,420** (built and measured) | cloud only | ~35 GiB |

The outboard blocks refine less spanwise (a far-field growth law), so the *global* ratio is
1.25 per level: cells grow ×1.96–2.03 rather than ×2.197. The desktop is a Windows PC with
15.86 GiB of RAM running Linux in WSL with 13 GB, so it holds `gci_C` and `gci_M` only.

**Directional variants** refine one direction at a time, for diagnosis:

| Level | Change |
|---|---|
| `gci_C_chord`, `gci_C_chord2` | chordwise points 45 → 58 → 75 |
| `gci_C_span` | spanwise 49 → 63 |
| `gci_C_normal` | wall-normal layers 65 → 84 at fixed s0 |
| `gci_C_s0` | first cell ÷ 1.3 |
| `gci_C_normal_s0`, `gci_C_normal_s0_2` | layers and s0 together, two steps |
| `gci_C_te` | trailing edge at 0.1 % of chord |
| `gci_C_ff100` | far field 100 chords (with 95 layers) |
| `gci_M_te` | medium level with trailing edge at 0.1 % |
| `m6_oh` | the ONERA M6 wing through our mesher |

### 2.3 The solver: ADflow, and every setting in force

ADflow is the University of Michigan MDO Lab's structured, cell-centred, finite-volume RANS code
(part of the MACH-Aero framework).

| Setting | Value | Why / evidence |
|---|---|---|
| Equations, model | RANS, Spalart–Allmaras (with the ft2 term) | standard for attached wing flow |
| Lift direction | `liftIndex 3` (z up) on AERIS meshes; checked by reading the freestream direction back **before** every solve | defect 14: the default of 2 applied angle of attack as sideslip |
| Nonlinear solver | **ANK only** (approximate Newton–Krylov, `--no-nk`), `ANKSubspaceSize` 10, `ANKPCILUFill` 1 | full Newton–Krylov froze at 1.1M cells with lean Krylov settings; ANK reproduced NK results to 5 decimals |
| Multigrid | single grid (`sg`) | 2-level multigrid 1.8× slower; 3-level impossible on O-H topology (B5) |
| Stopping rule | total residual down to **1e-6** relative; cap 30,000 cycles; time limit | 1e-8 bought nothing measurable (B2) |
| Spatial scheme | central differencing with **scalar** artificial dissipation, `vis2` 0.25, `vis4` 0.0156 (ADflow defaults) | **under test**: vis4 moves coarse drag by −21/+35 counts |
| Turbulence advection | first order (ADflow default) | second order changed CD −0.25 % (B8) |
| Freestream turbulence | archive: ADflow default `eddyVisInfRatio` 0.009 (SA χ ≈ 1.3). **Adopted for the re-run: 0.21 (χ = 3)**, NASA TMR's prescription | CD +0.88 % mean (range +0.46…+1.19 %), CL unchanged |
| Low-speed preconditioner | off | tested 15 Sept: no effect on the converged answer |
| Reference area | each geometry's **own** half-area (`reference_areas.json`) | defect 23 |
| Moment reference, chord | (0.40, 0, 0) m, 0.9 m, for every design | convention; see stability caveat (§8.3) |
| Half model | symmetry plane at the root, β = 0 | root plane flat to 8e-18 m |
| Ranks | 6 MPI ranks on the desktop | time-to-answer 827 s at 6 ranks for `gci_C` α 0 |

### 2.4 Is a run acceptable? The layered judgement

A run is accepted only if all of these hold:

1. **Solver guards** (`solve_s8.py`). At least 10 iterations; every force finite and inside
   physical bounds (e.g. 0 ≤ CD ≤ 0.5); the freestream direction and lift index read back
   and correct.
2. **Convergence gate** (`convergence_gate.py`), two routes:
   - *route 1*: the residual reached the target and the run was healthy;
   - *route 2*: ≥ 5 orders of residual drop, **and** forces settled over an adaptive tail
     window (CL < 0.05 %, CD < 0.1 %, |ΔCMy| < 1e-4 absolute, because CMy crosses zero), **and**
     no divergence or growing oscillation.

   A **frozen solver** — forces flat because the solver stopped stepping — is reported separately
   (`ACCEPTED_SOLVER_FROZEN`) and excluded from grid-convergence work.
3. **Per-equation convergence, Fluent's way** (12 Sept, PI direction). Each of the six equations
   is normalised by its largest residual in the first five iterations. Continuity, momentum and
   turbulence must each fall by 3 orders, energy by 6 (Fluent's defaults).
4. **Archive audit** (`audit_runs.py`), on every dataset row:
   - gate verdict and per-equation orders;
   - residual ≤ 1e-6;
   - the geometry's own reference area;
   - moment reference and chord;
   - zero folded cells, and the grid on the design surface;
   - **worst-cell y+ ≤ 1**;
   - ≥ 10 iterations;
   - freestream direction;
   - since 15 Sept, **friction plausibility**: CDv between 0.6 and 1.6 × a turbulent flat plate
     at the design's Reynolds number.

   That last check exists because a broken turbulence-model variant passed everything else.

### 2.5 From run to dataset

`dataset_row.py` writes one row per accepted solve with 53+ fields:
- identity and hashes;
- the design vector;
- mesher, level and cell count;
- git commit and solver options *as read back from the solver*;
- operating point and each design's own Reynolds numbers;
- CL, CD, CDp, CDv, CMx/y/z;
- every convergence measure;
- mesh state, y+ percentiles, cp-bound checks;
- paths to surface and volume fields (for future AI field prediction);
- the paired AVL result.

`collect_dataset.py` builds `05_s6_cfd_qualification/dataset/` (69 rows today) and refuses
unjudged or rejected runs.

### 2.6 Orchestration: how runs are launched and protected

- `run_campaign.py` runs a pilot level over the ten geometries. It holds an exclusive lock (one
  measurement at a time), passes each geometry's own area, refuses a geometry without one, and
  runs a regression check first.
- **Queues** (`artifacts/s8_checklist/queueNN.sh`) chain long test sequences overnight:
  - they skip any run that already has a result, so a crash loses nothing;
  - they kill a run cleanly if free memory falls below ~2 GiB, or at a time limit;
  - a **decision gate** stops before expensive steps if an earlier test moved drag by ≥ 1 count.
- `cloud_preflight.py` and `cloud_run.sh` are the cloud path. Preflight checks reference areas,
  memory, environment and authorization before anything is billed. The run builds meshes in the
  cloud (12 s each) rather than uploading them, and ends with the archive audit.
- `select_levels.py` picks grid levels from *available* memory, not installed memory.

### 2.7 The multi-fidelity idea, in one paragraph

AVL, a vortex-lattice method, is inviscid and thin, but it takes seconds. CFD takes hours. On
the ten pilot wings, AVL's lift differs from CFD's by a nearly **constant offset at each angle of
attack**. So the plan: learn that offset from the CFD designs, add it to AVL everywhere. Tested
by leaving one wing out, ten times (D6):
- lift error falls from rms 0.022 to **0.004**;
- the neutral point from 20 mm to **2.8 mm**.

Drag needs its own model, since AVL gives only induced drag. The part AVL cannot supply varies
only 3.8–4.7 % between wings at each angle, which a simple model can carry.

---

## 3. Chronology

Dates are 2026. Commit titles are in `git log`; this is the story they tell.

### Before S8 — 30 August to 3 September: the S6 qualification

A governed qualification scaffold (M0–M2 gates, mission authority, classification thresholds)
and one measured CFD "canary" on the C-family candidate C03. It ran out of memory twice and
completed on 2 September. The postmortem found an interface defect traced to the leading-edge
collar, starved wrap resolution in all 65 meshes, and a pyHyp marching envelope that failed at
the tip cap. On 3 September the PI asked *"in the general roadmap where are we? and why we
failed?"* and set the pace: *"we must be able to produce the paper within this month."*

### Phase 1 — 4–5 September: S8 is born and survives its first defects

- **First S8 grid and first CFD point** (ADR-0018).
- **Defect 14 — the lift axis.** `liftIndex` was omitted, so ADflow applied the angle of attack
  as **sideslip**; every early result was void. Caught because α 8 reported a stagnation cp of
  0.007. Fixed, and now checked before every solve (ADR-0019), together with defect 15
  (leading-edge spacing jumping 2.6× between stations).
- **The corrected α sweep** on design 83: three angles converged, α 4 froze (Newton step 0,
  linear residual 1.000).
- **An output-based convergence gate** replaced a bare 1e-8 residual cutoff: forces settled *and*
  residual drop *and* health, with frozen solvers flagged.
- **AVL on the identical loft.** Lift agreed to 1.9 % at α 8. A "stability disagreement" was
  two sign errors of the assistant's own (defect 17), caught by the PI noticing mirror-image
  moment curves.
- **100-design robustness screen.** 15 designs folded cells. Defect 19: the fold check had been
  2D per section, and folds were *between* sections. Mechanism found: the marching plane
  tilted up to 8° (defect 20). Fix: span-normal marching. Its first version moved the wing
  surface by 1.2 mm while every quality metric stayed unchanged (defect 21), so the wall offset
  was restored and guarded. **Result: 100/100 designs clean.**
- **The grid family** is generated from one ratio (r = 1.3) instead of by hand; its first
  audit for external review corrected two overclaims.

### Phase 2 — 6 September: the desktop campaign plan and the first refinement

- `PLAN_desktop_campaign.md`, revised after an external review; authorized as ADR-0020 and policy
  `s8_campaign_v1` for **two levels only** (the host cannot hold a third — "a trend, not a GCI.
  Say so").
- Four-angle sweep on `gci_C`; defect 22 (AVL moment arm applied twice).
- **Stopping rule 1e-8 → 1e-6**, after the PI pointed out *"we decided that 10^-6 is enough to
  accept... update the policy and follow it throughout."* It was measured: forces move ≤ 5e-5
  beyond 1e-6, and the sweep took 63 minutes instead of 308.
- **ONERA M6 validation** on NASA's grid, following the PI's link to the NASA data.
- **`gci_C` → `gci_M` on design 83**: lift and neutral point converge; pressure drag does not.
  Far field 40 → 60 chords is worth only 0.33 % of drag.
- A trusted-results archive outside the wiped `artifacts/` tree. The PI's direction: save
  everything needed *"for reproducability and validation."*

### Phase 3 — 8–9 September: generalization and cloud readiness

- The gate's two criteria had been wired as one hurdle; measured, then fixed.
- **Ten-geometry pilot**: 10/10 meshes clean, 40/40 runs accepted. The coarse→medium
  pressure-drag offset looks constant.
- **Cloud-ready**: signed policy `s8_cloud_hf_v1` for `gci_F` on ten wings and `gci_FF` on design
  83, a preflight that bills nothing, a bootstrap script.
- Edge refinement refuted as the cause of the drag offset (7 % of it). The 1e-6 rule revalidated
  on three other geometries (≤ 0.073 % movement).
- The PI asked whether to switch to SU2, and asked for *"every metric, every refinement option"*
  checked before the cloud, *"a fluent meshing style option."*

### Phase 4 — 10–11 September: where the drag error lives, and a big defect

- **Directional refinement**: chordwise resolution carries **88 %** of the pressure-drag offset,
  spanwise 11 %, edges 7 %, wall-normal the other way.
- **Chordwise three-level GCI**: pressure drag converges monotonically, observed order ~3.25.
- **Defect 23**: every pilot geometry had been normalised by design 83's area. Nine wings'
  coefficients were scaled by −25 to +31 %, and a published conclusion — that the CFD–AVL
  correction is geometry-specific — was an artefact. Retracted; the true agreement is ±2 %.
- First-cell height exonerated. Rank scaling measured.
- **Pre-cloud checklist**: area preflight, `gci_FF` built (4,504,420 cells), per-design CG
  limits, surrogate check, transition bound. M6 re-run in the campaign configuration. NACA 0012
  verification set up.
- **Every setting on trial**: multigrid rejected; the freestream-turbulence recommendation to
  keep the default withdrawn.

### Phase 5 — 12–13 September: rigor, published practice, and the tip cap

- Time-to-answer scaling: 2.56× on 6 ranks, 43 % efficiency. A force sanity check added after
  Menter SST reported CL −1.17 as converged.
- **Per-equation convergence**, Fluent's way, at the PI's direction: *"lets follow fluent
  process... check that every passed run has all the variables indeed converged."* All 77 runs
  cleared it.
- χ = 3 freestream turbulence against the default. Archive audit; label defects 24 and 25.
- **Geometry audit**, design vector → built mesh (its twist check was later found to be wrong;
  see Phase 6).
- **The ONERA M6 through our own mesher** — the test that puts *our mesh*, not only our solver,
  in front of a measurement.
- **Tip cap wall-resolved.** The PI asked how the mesh is built, established that the tip is
  airfoil-shaped, not square, and ruled: *"read the cfd/meshing textbooks... we should mandatory
  achieve <1."* Tip-cap cell 10 → 2 × s0; cap y+ 4.2 → 0.85; builds refuse above 3.
- **SU2 reads our mesh** (a second, independent solver). Each design's own Reynolds number
  recorded. Two unchosen defaults put under test.
- **Acceptance criteria from published practice**: AIAA Drag Prediction and High-Lift Prediction
  Workshops, NASA TMR, Celik 2008, ASME V&V 20. The PI's condition: *"make sure these data
  refer to similar cases and not supersonic."* Result: gridding rules apply at low speed, but
  our validation cases did not match our regime. **NACA 4412** is the one that does.

### Phase 6 — 13–15 September: a restart, corrections, results, and a challenged run list

- **13 Sept 23:23: the WSL virtual machine restarted** (most likely memory, during SU2 on six
  ranks) and killed every queued job. The chain was rebuilt resumable and memory-guarded.
- **15 Sept, early.** Five measurements that had answered a different question than asked were
  found and corrected (all tools written by the assistant):
  - the M6 comparison sliced stations beside their true span position, and let tip-cap cells
    into every slice;
  - its shock detector matched different legs of a lambda shock;
  - the guideline checker read leading-edge spacing on one ring picked by index;
  - wall-normal refinement had been judged on pressure drag alone;
  - the twist audit measured from a trailing-edge corner on the tip closure. **The wing has the
    twist it was asked for.**
- **NACA 4412 case built**; queue17 results arrive (§4, G1–G6). The wall-resolved tip cap moves
  drag +1.5 / +1.7 / +2.3 counts. **Re-run approved** by the PI with the new cap.
- **SU2 on the wing** disagreed with ADflow on friction by +22–27 %. SU2 on NACA 0012, a case
  with a known answer, showed **SU2's own friction is 15 % high**, so the problem is SU2's setup,
  not ADflow.
- The PI: *"why we qued all these runs? what is the purpose. i want you to challenge the run
  list."* The challenge cut ~17 h of runs that could not change a decision. The re-run went on
  hold, because dissipation (−21/+35 counts) and trailing-edge spacing (−10) move drag far
  more than the tip cap. The PI agreed.
- **NACA 4412 results**: friction, separation point and velocity profiles match NASA's CFL3D.
  Finest grid stalled.
- **Third wall-normal level: drag rises faster with each refinement** (not converging in that
  direction). Matrix dissipation stalled under ANK; retried with Newton–Krylov.
- A GitHub deploy key made pushing work from the desktop.

---

## 4. Study catalogue

Format: **question → method → result → decision → confidence.** Confidence words:
- *Trusted* — verified independently;
- *Indicative* — real but with a stated weakness;
- *Open* — not yet answered.

### A. Mesh

| # | Question | Result | Confidence |
|---|---|---|---|
| A1 | Is every mesh valid and repeatable? | 0 folded cells on every build; wall 2.2e-16 m from the loft; 37/37 metrics identical across two independent builds | Trusted |
| A2 | Does it work across the design space? | 100/100 designs at `gci_C` after the fold fix; o_wing block shape identical on all | Trusted |
| A3 | How much memory per level? | ANK→NK: 1.51 + 9.46 GiB per million cells; ANK-only 2.69 + 7.23 (two-point fits, +6 % check error) | Usable with margin |
| A4 | Does the built wing match the design vector? | root chord, span, tip ratio, sweep all within tolerance; twist within 0.2° at every station on 9 of 10 wings; g12 +0.25° where its twist has a sharp V at a span break, rounded by the loft spline (0.15°) and the mesh rows (0.09°). 49/50 | Trusted |
| A5 | Does the mesh meet published gridding guidelines? | coarse 3/10, medium 5/10 (§5.3): no constant-spacing wall layers, 20 boundary-layer cells at coarse, trailing edge 0.40 % vs 0.1 %, far field 40 vs ~100 chords, leading edge 0.17 % at the tip | Measured; the gaps are being tested |
| A6 | Is the tip cap wall-resolved? | was y+ 2.9–4.5; since 13 Sept y+ ≤ 0.85; refused above 3 × s0 | Trusted |

### B. Solver configuration and convergence

| # | Question | Result | Confidence |
|---|---|---|---|
| B1 | Did the frame fix change forces? | CL moved 2.75e-4, CD 1.5e-5 (regression check) | Trusted |
| B2 | Is 1e-6 enough? | continuing 20–40× further moves forces ≤ 0.073 %; two paths to 1e-6 differ by 0.32 % in CDp — the "path noise" floor | Trusted |
| B3 | Does the gate catch frozen solvers? | all three known frozen runs still caught after every gate change | Trusted |
| B4 | ANK-only instead of ANK→NK? | reproduces NK results to 5 decimals; NK froze at 1.1M cells | Trusted for wings |
| B5 | Multigrid? | 2 levels 1.8× slower; 3 levels unsupported on this topology | Rejected |
| B6 | Freestream turbulence χ 1.3 vs 3? | CD +0.88 % mean over 44 wing/angle pairs (+0.46…+1.19 %), CDv +1.8 %, CL −0.07 %; the same on `gci_M` for design 83 | Trusted; **χ 3 adopted** |
| B7 | Does every equation converge? | over 77 runs the turbulence equation lags (3.3–5.3 orders) but all pass Fluent's limits; a 1e-4 turbulence limit would reject 48 runs that are demonstrably converged | Trusted |
| B8 | Time to an answer vs ranks | 2121 / 1121 / 849 / 827 s at 1 / 2 / 4 / 6 ranks; core-hours per point rise 0.59 → 1.38 | Trusted; cheapest cloud use is many small cases |

### C. Grid refinement (design 83 unless stated)

| # | Question | Result | Confidence |
|---|---|---|---|
| C1 | Coarse → medium, four angles | α 0: CD 208.2 → 179.8 counts (−28.4), CDp 118.3 → 90.8, CDv 90.0 → 89.1, CL +0.14 %, CMy −0.8 %. Two levels = **trend, not GCI** | Trusted as a trend |
| C2 | Neutral point | 0.3557 → 0.3552 m (0.05 % of chord) | Trusted as a location |
| C3 | Far field 40 → 60 chords | CD +0.33 % (0.7 counts) at α 0, +0.23 % at α 8 | Trusted |
| C4 | Leading/trailing-edge spacing together, same cells | recovers 7 % of the drag offset | Trusted, but see G4 |
| C5 | Which direction carries the drag error? | of the CDp offset: chord **88 %**, span 11 %, edges 7 %, wall-normal −15 %. On *total* drag: layers 65 → 84 +1.7 counts, s0 ÷ 1.3 −0.8, both +0.9 | Trusted for design 83 |
| C6 | Chordwise three-level GCI | CD extrapolates to 167.8 counts (α 0), 158.8 (α 4), GCI ~5.5 %; observed order ~3.25 — above the scheme's formal 2 | **Indicative** |
| C7 | First-cell height alone | recovers ~1 % of the offset | Trusted: not the error |
| C8 | Wall-normal third level (15 Sept) | α 0: CD 208.2 → 209.1 → **213.3**; α 4: 198.6 → 199.4 → **203.1**. The step *grows*: CDp rises 118 → 122 → 129 while CDv falls 90 → 87 → 85. **Not converging** in that direction at fixed chordwise resolution | **Open — important** |
| C9 | `gci_FF` real size | 4,504,420 cells, 0 folded, 1.6 GB to build | Trusted |

### D. Across geometries

| # | Question | Result | Confidence |
|---|---|---|---|
| D1 | Pilot robustness | 10/10 meshes, 40/40 `gci_C` runs accepted; 20/20 `gci_M` runs on five wings | Trusted |
| D2 | Is the coarse→medium CDp offset the same on every wing? | re-derived for this report over **five wings including the smallest (g47, 0.298 m²) and largest (g13, 0.516 m²)**: mean −0.00284, sd 0.00020 (**7 %**), range −0.00244 (g65) … −0.00332 (g47 α 8); total drag −25.6 … −34.0 counts | Trusted: near-constant, not exactly |
| D3 | y+ on every planform | wing p99 0.68–0.83 on all ten | Trusted |
| D4 | CFD vs AVL lift | ±2 % on every wing after defect 23 | Trusted as corrected |
| D5 | Stability | neutral points 0.24–0.39 m (22.7–29.4 % MAC), all forward of the 0.40 m reference, so every design would be unstable with its CG there; CFD neutral point 16–26 mm aft of AVL's | Trusted as aerodynamics; no stability verdict without a mass model |
| D6 | AVL + learned offset on an unseen wing | CL rms 0.022 → 0.004; neutral point 20 → 2.8 mm | Supported (one level, ten wings) |

### E. Physics checks

| # | Question | Result | Confidence |
|---|---|---|---|
| E1 | Is mass conserved? | far-field imbalance 7e-8 to 1.3e-7 of throughput | Trusted |
| E2 | Surface cp above its physical bound (1.0018)? | a few cells at the blunt-TE corner (a geometric kink, 0.01–0.2 % of CDp) and at the leading edge at α 8; peak excess falls 33–72 % with refinement — discretization | Trusted |

### F. Verification and validation against external answers

| # | Case | What it tests | Result | Confidence |
|---|---|---|---|---|
| F1 | **ONERA M6** on NASA's grid (295k), M 0.84, Re 11.7 M, α 3.06° | solver setup vs measured wing pressures (transonic, **not our regime**) | suction peak within 1.1 %, shock position 0.014 x/c, RMS Δcp 0.15; coarser NASA grids oscillate; integrated measured normal force +5.3 % | Qualitative |
| F1b | **ONERA M6 through our mesher** (955k) | our *mesh* vs measurement | RMS Δcp **0.074** (better than NASA's grid), suction peak 2.6 %, shock 0.021 x/c; shocks smeared at mid-chord; tip station suction 7 % high | Against our pre-set criteria: suction peak **not met** (2 %), shock **marginal** (0.02) |
| F2 | Transition, bounded | how wrong "fully turbulent" could be | NeuralFoil on 88 sections: turbulent/free-transition section drag median **1.57** (0.46–1.97; below 1 at tips with laminar bubbles) | An estimate that sizes the question |
| F3 | **NACA 0012** (NASA TMR), M 0.15, Re 6 M, α 10°, our own pyHyp grids | verification against an answer three codes agree on | friction CDv +0.3–0.6 %; CDp +8.6–9.5 % on o512, converging; two-grid extrapolated CD within 0.41 %; CL +0.7 %; AERIS Mach 0.0837 vs 0.15: CL −0.45 %, CD +1.3 %; finest grid would not converge (pinched TE cell) | Trusted for friction; pressure drag converging |
| F4 | **NACA 4412** (TMR 2DN44), M 0.09, **Re 1.52 M on 0.90 m**, α 13.87°, trailing-edge separation | our regime, with experiment | friction inside the CFL3D–FUN3D band; separation x/c **0.790 vs CFL3D 0.789**; velocity profiles within 0.006–0.010 of CFL3D (both 0.05–0.12 from the experiment — the SA model and tunnel); o512 CL +1.45 %, CD +4.1 % of the band (limits: 1 %, 3 % *after extrapolation*); o1024 stalled | Friction/separation **trusted**; forces **open** |

### G. Settings on trial, 15 September (design 83, coarse level, one change at a time)

| # | Change | Drag effect | Reading | Decision |
|---|---|---|---|---|
| G1 | Tip cap wall-resolved | +1.5 / +1.7 / +2.3 counts (α 0/4/8) | real and ≥ 1 count | **adopted** |
| G2 | Low-speed preconditioner | 0.00 | does not change the converged answer | dropped |
| G3 | **vis4 halved / doubled** | **−20.6 / +35.1 counts** (CDp −17 / +29 %) | artificial dissipation is a large part of coarse drag | **pending** |
| G4 | **Trailing edge 0.1 % of chord** | −9.0 / −9.2 counts; **≈ −10.5 / −10.9 without the tip cap's share** (the level carried the new cap) | the trailing edge matters a lot | **pending** (medium-mesh test tonight) |
| G5 | Far field 100 chords (with 95 layers) | +5.7 / +6.9; ≈ +4.2 / +4.7 without the cap — still mixed with 30 extra wall layers | cannot be attributed | clean test tonight |
| G6 | SA-Edwards model variant | −60 counts, friction −66 % | a laminar-like boundary layer that every check accepted | dropped; friction check added |
| G7 | Matrix dissipation (ANK) | stalled: CFL stuck at 42–88, residual flat | cannot converge with ANK | retry with NK tonight |
| earlier | SA without ft2 / second-order turbulence advection / Menter SST | +0.16 % / −0.25 % / aborts in this ADflow build | negligible / negligible / unusable | keep defaults; no second model yet |

### H. Cross-code

| # | Question | Result | Confidence |
|---|---|---|---|
| H1 | SU2 on the same wing mesh (unconverged, 4 h limit) | friction +22 / +27 % above ADflow; pressure drag 67 vs 119 counts; lift within 1–4 % | Not a result |
| H2 | SU2 on NACA 0012 with the same numerics | friction **+15.5 %** from NASA's answer, where ADflow is +0.6 % | Trusted: **SU2's setup is at fault** |
| H3 | Which SU2 setting? | least-squares gradients, second-order turbulence, Roe scheme: first runs unconverged after 1 h; re-queued to run to convergence | Open |

---

## 5. The rulebook

### 5.1 Scales to keep in mind

| Quantity | Size |
|---|---|
| One drag count | 1e-4 CD ≈ **0.5 %** of our CD |
| Cross-code scatter at drag workshops | 4–5 counts |
| Path noise of the 1e-6 rule | 0.32 % of CDp ≈ 0.6 counts |
| Decision threshold used by the queues | **1 count** |
| Coarse → medium drag change | 26–34 counts |
| Dissipation sensitivity (vis4 ×½ / ×2) | −21 / +35 counts |

### 5.2 Operating point and references

Given in §1.1. Areas: each geometry's own half-area from the loft, 0.298–0.516 m²; mesh-derived
areas agree within 0.21 %. Moment reference x = 0.40 m, chord 0.9 m for all designs.

### 5.3 Mesh guidelines against our levels (design 83)

| Criterion | Published | Coarse `gci_C` | Medium `gci_M` | Status |
|---|---|---|---|---|
| y+, every wall cell | ≤ 1 | 0.86 wing, 0.85 cap | 0.73, 0.85 | met (since the cap fix) |
| Growth in the viscous layer | < 1.25 | 1.253 | 1.190 | coarse marginal |
| Constant-spacing layers at the wall | ≥ 2 | 0 | 0 | **not met** |
| Cells inside the boundary layer | ≥ 30 (practice) | 20 | 26 | **not met**; F 33, FF 43 |
| Leading-edge spacing, worst span row | ~0.1 % chord (≤ 0.15 accepted) | 0.166 % at the tip; 0.05–0.07 % inboard | 0.140 % | coarse **not met** outboard |
| Trailing-edge spacing | ~0.1 % chord | 0.40 % | 0.31 % | **not met** — worth ~10 counts (G4) |
| Spanwise at tip / root | ~0.1 % semispan | 0.005 % / 2.04 % | 0.004 % / 1.59 % | tip met; root deviation (symmetry plane, not a wing-body junction) |
| Far field | ~100 chords | 40 | 40 | deviation, being measured |
| Refinement ratio | ≥ 1.3 (Celik); ×3 cells per level (High-Lift Workshop) | r = 1.3 | | at the minimum |
| Folded cells | 0 | 0 | 0 | met |

Sources: AIAA Drag Prediction Workshop gridding guidelines (DPW 3–7), AIAA High-Lift
Prediction Workshop (M 0.2, Re 4.3 M — the same numbers at low speed), NASA TMR, Celik et al.
2008. `mesh_guidelines.py` measures these on any built mesh.

### 5.4 Convergence

| Rule | Value |
|---|---|
| Stopping | total relative residual 1e-6 (strict mode `--l2 1e-7` or tighter for verification) |
| Gate route 2 | ≥ 5 orders; CL < 0.05 %, CD < 0.1 %, \|ΔCMy\| < 1e-4 over the tail; no divergence or growing oscillation |
| Per equation | continuity, momentum ×3, turbulence ≥ 3 orders; energy ≥ 6 (normalised by max of first 5 iterations) |
| Minimum iterations | 10 |
| Force bounds | CL, CMx/y/z in ±3; 0 ≤ CD ≤ 0.5; −0.05 ≤ CDp ≤ 0.5; 0 ≤ CDv ≤ 0.1 |
| Frozen solver | reported separately; excluded from grid convergence |

### 5.5 Data acceptance (every archived row)

All of §5.4, plus:
- the geometry's own reference area within 0.5 %;
- moment reference (0.40, 0, 0) and chord 0.9;
- no folded cells, and the wall within 1e-9 m of the design surface;
- **worst-cell y+ ≤ 1**;
- freestream direction error < 1e-9;
- **friction 0.6–1.6 × a turbulent flat plate** at the design's MAC Reynolds number, on 2.04 ×
  planform area. The 69 archived rows read 0.91–1.02; SA-Edwards read 0.33.

### 5.6 Grid convergence

- Three levels at r ≥ 1.3 and a GCI on the finest pair.
- Observed order reported; inside [1, 3] it is a band, outside it is *indicative*.
- `gci.py` solves the unequal-ratio Richardson relation numerically (verified to 1e-10 on
  manufactured solutions).
- It refuses frozen runs, and refuses to emit a band from two levels.

### 5.7 Validation and cross-code criteria

These were set before results where possible:

| Case | Criterion | Status |
|---|---|---|
| ONERA M6, our mesh | suction peak within 2 %; shock within 0.02 x/c; sectional load within 5 % at stations 2–6 | 2.6 % (not met); 0.021 (marginal); loads not yet integrated |
| NACA 0012 | lift within 0.5 %, drag within 2 % after extrapolation | extrapolated drag within 0.41 %; lift +0.7–0.9 % |
| NACA 4412 | CL within 1 %, CD within 3 % of the CFL3D–FUN3D band after extrapolation; separation within 0.05 x/c of 0.79 | separation met; forces await o1024 |
| Cross-code (SU2) | within 5 drag counts on the same mesh | not assessable until SU2 matches NACA 0012 |
| Model form | SA against a second model, as a band; transition carried as a bound | no second model; transition bound 1.4–2× |

What we will be able to say about drag is a **chain**, never a single number:
1. the solver reproduces known low-speed friction and drag;
2. our mesh reproduces measured pressures on a real wing;
3. the grid error is bounded by the GCI (after the cloud);
4. model-form error is bounded by the transition and turbulence-model bands.

### 5.8 Governance

- Heavy compute needs a signed policy with a scope limit: `s8_campaign_v1` (desktop) and
  `s8_cloud_hf_v1` (cloud). Authorizations given in chat are recorded as such — "written into
  the record by the agent that was directed, not typed by the principal investigator."
- The hold-out set is locked.
- No paper claim is authorized by any current policy.

---

## 6. Safeguards and fallbacks

### 6.1 Checks that fail closed — they stop the run rather than warn

| Guard | What it prevents | Born from |
|---|---|---|
| Freestream direction and lift index read back before solving | angle of attack silently applied as sideslip | defect 14 |
| Wall layer within 1e-9 m of the loft | a wing silently deformed by a mesh "fix" | defect 21 |
| Signed volume of every assembled hex, smoothing ladder, then raise | writing a folded mesh and exiting 0 | defect 19 |
| Tip-cap cell ≤ 3 × s0 | a wall that is not wall-resolved | tip cap, 13 Sept |
| Own reference area, refusal if missing (campaign, preflight, AVL pairing) | coefficients scaled by the wrong area | defect 23 |
| Minimum 10 iterations and physical force bounds | a crashed turbulence model reporting "converged" | SST, 12 Sept |
| Frozen-solver detection in the gate | flat forces from a stopped solver counted as converged | α 4, 5 Sept |
| Per-equation limits | one lagging equation hidden inside the total residual | 12 Sept |
| Archive audit incl. worst-cell y+ and friction plausibility | a defect surviving into training data | 13 and 15 Sept |
| Exclusive run lock | two measurements contaminating each other | PLAN §0.2 |
| Level selection from available memory | a job that cannot fit | 6 Sept |

### 6.2 Fallbacks — what happens when the preferred path fails

| If… | then… |
|---|---|
| Newton–Krylov freezes | ANK only (the campaign default); verification cases use stronger NK settings |
| ANK stalls (NACA cases, matrix dissipation) | NK with stronger preconditioning (`NKADPC`, more subspace, outer preconditioner iterations) |
| a mesh folds | the smoothing ladder; the old SVD frame stays selectable; failure raises |
| the host cannot hold a third level | `gci_MF` (a half-step) on 14–26 GiB hosts; below 14 GiB, two levels reported as a **trend**; otherwise the cloud |
| a run crashes or the machine restarts | queues are resumable; completed runs are never repeated |
| memory runs low | the queue kills the run cleanly below ~2 GiB free |
| a test moves drag by ≥ 1 count | the queue stops before the expensive step and waits for a decision |
| a validation claim needs more rigor | a strict residual mode, and NASA TMR cases with known answers |
| a second opinion on the solver is needed | SU2 (setup being fixed) |
| S8 ever fails on a design | the designated fallback mesher is **deformation of a validated S8 mesh**, not S6 (which carries the collar defect); both paths would be run on ~10 designs first to measure their offset |

### 6.3 Operational traps this project learned

- Never change code or run a second job during a measurement.
- Exit code 0 is not success.
- A quality metric that passes is not the metric that matters (position vs shape).
- Solver defaults are not neutral choices.
- `pkill -f` can kill your own shell.
- ADflow writes ADF-format CGNS, which h5py reads as nothing.
- The WSL VM can restart and take every job with it.
- Measure a mesh quantity on every span row, never one picked by index.
- Measure twist from the middle of the opened trailing edge.

Each is recorded in `STATE_2026-09-10.md` with its cost.

---

## 7. Where we are now and what is left

### 7.1 Running tonight (queue20, started 20:39)

| # | Test | Question | ETA |
|---|---|---|---|
| 1 | Matrix dissipation, coarse α 0, NK allowed | does a less dissipative scheme converge, and how much drag does it remove? | ~22:10 |
| 2 | vis4 halved on the medium mesh | does the dissipation effect shrink with refinement, as it should? | ~22:50 |
| 3 | Trailing edge 0.1 % on the medium mesh | is the ~10-count effect a coarse-mesh artefact or a setting every level needs? | ~23:35 |
| 4 | Far field 100 chords at 65 layers, α 0 and 8 | the far-field effect alone | ~00:15 |
| 5 | Matrix dissipation α 4 and medium — only if #1 converges | robustness and grid dependence | ~02:15 |
| 6 | NACA 4412 finest grid, stronger NK | the third level for extrapolation | ~03:00–06:00 |
| 7 | SU2 numerics variants on NACA 0012, to convergence | which setting fixes SU2's friction | ~05:00–10:00 |

### 7.2 Decisions waiting for the PI (Wednesday)

1. **Dissipation**: ADflow defaults, a lower vis4, or matrix dissipation (if it converges).
2. **Trailing-edge spacing**: 0.1 % of chord at every level, or not.
3. **Far field**: 40 or 100 chords (100 needs ~30 more layers to keep growth under 1.25).
4. **Wall-normal resolution**: C8 shows no convergence at fixed chordwise resolution; whether
   to add constant-spacing wall layers and reach ≥ 30 boundary-layer cells.
5. Then: **the re-run** (approved for the tip cap; ~17 h: ten wings coarse, three wings medium).
6. Later: a **mass model per design** (stability verdicts), **fixed angle vs fixed lift**
   datasets, the **cloud budget and concurrency**.

### 7.3 Before the cloud batch

- Amend `s8_cloud_hf_v1`: it predates χ = 3, the wall-resolved cap and tonight's settings.
- Correct the cloud time estimate: it assumes 75 % efficiency on 24 ranks, but we measured 43 %
  on 6. Use many cases at 2–4 ranks each.
- Size `gci_FF` from the measured 4,504,420 cells, not the 4.7M estimate still in
  `s8_cloud_batch.json`.
- SU2 fixed on NACA 0012, so criterion E can be judged.
- The dataset v2 audit passing on the re-run.

### 7.4 The cloud batch and after

1. `gci_F` on the ten wings, `gci_FF` on design 83 — 44 cases.
2. With `gci_C` / `gci_M` / `gci_F` / `gci_FF` on design 83: **the first real GCI** (observed
   order, extrapolated drag, uncertainty band).
3. The fine-level offset across ten wings.
4. The multi-fidelity model trained with stated uncertainty.
5. A design-space campaign of 100–200 designs. At today's ~14 minutes per coarse point on six ranks a
   four-angle polar is about an hour, so 100 designs is about four days of desktop solving (the
   plan's original eight days assumed 30 minutes per point).

### 7.5 Longer-term gaps

- Transition: ADflow has none; SU2 has transition models.
- A working second turbulence model (SST in SU2).
- A low-speed 3D wing experiment — none public for a BWB.
- The alignment of dataset angles with trimmed flight.

---

## 8. Critical review

### 8.1 Re-derived while writing this report

| Claim | Re-derived | Verdict |
|---|---|---|
| Archive "69/69 sound" (AUDIT G2, 13 Sept) | re-audited under the worst-cell y+ rule adopted the same day: **0/69 sound**, every row failing only on tip-cap y+ (up to 4.16) | the archive **cannot be training data**; the re-run is required, not optional |
| CDp offset "provisional — three wings of the same size" (AUDIT D2) | five wings incl. smallest and largest: mean −0.00284, sd 7 %, range −0.00244…−0.00332 | holds across size, within ±7 % |
| χ 3 effect "+0.88 %" (VV table) vs "+1.19 %" (AUDIT B6) | 0.88 % is the mean of 44 pairs (range 0.46–1.19 %); 1.19 % is design 83 α 0 | both right; now labelled |
| Cloud batch 23.8 solver-hours | assumes 75 % efficiency at 24 ranks | contradicted by measurement (43 % at 6); **optimistic, possibly 2–3×** |
| Friction check band | ADflow SA 0.91–1.02 on 69 rows; SA-Edwards 0.33; unconverged SU2 1.19 | band is safely wide and catches the known failure |

### 8.2 Statements in older documents that are stale or wrong

| Where | Says | Correct reading |
|---|---|---|
| `AUDIT_2026-09-10` verdict table / G2 | archive 69/69 sound | 0/69 under the adopted y+ rule (§8.1) |
| `AUDIT_2026-09-10` D2 | offset provisional, same-size wings | holds on five wings incl. size extremes (§4 D2) |
| `AUDIT_2026-09-10` D3, `STATE` | tip cap y+ 2.9–4.5 "by design" | superseded: y+ ≤ 1 mandatory, cap fixed 13 Sept |
| `AUDIT_2026-09-10` Settings table and Next steps (11 Sept) | many items "testing (queueN)" | results are in B8 and the 15 Sept section; list superseded by §7 here |
| `VV_ACCEPTANCE` final table, row 4 | wall-normal refinement "moves CDp the wrong way (+3.3 %)" | on total drag +0.9 then +4.2 counts: **not converging** (C8) |
| `VV_ACCEPTANCE` final table, row 5 | leading edge 0.096 %, "met, keep" | 0.166 % at the tip on the coarse mesh (the upper table was corrected, the final table was not) |
| `VV_ACCEPTANCE` final table, row 6 | trailing edge "recovered 7 % ≈ 2 counts" | trailing edge at 0.1 % ≈ −10.5 counts (G4) |
| `VV_ACCEPTANCE` row 9 and "what is being done" | far field and TE tests in `queue16` | the far-field test was confounded; clean test in queue20 |
| `VV_ACCEPTANCE` criterion E | SU2 "queued" | SU2's friction is 15 % off on NACA 0012; E cannot be judged until fixed |
| `VV_ACCEPTANCE` criterion G | "χ 3 adopted and re-running" | adopted; re-run on hold |
| `PLAN_desktop_campaign` §5 | "validate the solver configuration, not the mesher" | the mesher was also put in front of measurement (M6 on our mesh) |
| `s8_cloud_batch.json` | 24 ranks at 75 %; `gci_FF` 4.7M unmeasured | 43 % at 6 ranks; 4,504,420 measured |
| `s8_cloud_hf_v1` policy | pre-dates χ 3, the cap and the settings decision | must be amended before any cloud run |
| `viz/s8_onera_m6_validation_20260906.png` | M6 comparison figure | drawn with the station-slicing bug; replaced by `viz/s8_onera_m6_nasa_grid_20260915.png` and `viz/s8_onera_m6_our_mesh_20260915.png` |
| queue18 printout | "gci_C is +0.24 counts from the extrapolated value" | meaningless: the observed order was negative (drag diverging) |
| Chat, 15 Sept 08:50 | "SU2's forces had stopped changing" | wrong: its drag was still falling ~3 counts per 500 iterations |
| Chat, 15 Sept 18:30 | "SU2 variants take about 5 minutes" | wrong: each needs 1–3 hours |

### 8.3 The challenge: weaknesses a reviewer should press on

1. **The dataset's drag is mostly a numerical quantity at the coarse level.** Three pieces of
   evidence:
   - coarse → medium removes 26–34 counts;
   - dissipation alone swings it by −21/+35;
   - trailing-edge spacing by −10.

   The offset is near-constant (±7 %) across wings, which is what makes multi-fidelity
   correction possible. But ±7 % of 28 counts is ±2 counts of wing-to-wing noise that the coarse
   data cannot resolve. Any drag ranking of designs at the coarse level must carry that.
2. **Grid convergence of drag is not demonstrated.**
   - Two levels give a trend.
   - The chordwise GCI's observed order (3.25) is too high to be trusted as asymptotic.
   - The wall-normal direction *diverges* at fixed chordwise resolution. That is plausible if
     scalar dissipation grows with cell aspect ratio, but that is a hypothesis tonight tests.
   - **Risk for the cloud:** a family whose directions converge in opposite senses may converge
     non-monotonically, and then no GCI band can be quoted. Rechecking this on `gci_F` is the
     cloud batch's first job.
3. **Model-form error dominates the unknowns.**
   - Fully turbulent SA at Re ~1 million per MAC could overstate profile drag by a median factor
     of 1.57.
   - No second turbulence model runs in ADflow.
   - SU2 is not yet trustworthy.
   - NACA 4412 shows ADflow reproduces CFL3D's SA solution, *including CFL3D's disagreement with
     the experiment*.
4. **Validation is regime-limited.**
   - The M6 is transonic.
   - NACA 0012 and 4412 are 2D.
   - No measurement exists for any BWB like AERIS.

   The honest claim is the chain in §5.7, never "S8 predicts AERIS drag to X %".
5. **Much rests on design 83 and two angles.** The directional study, the settings trials and the
   third wall-normal level were each done on one wing, usually at α 0 and 4. The pilot confirms
   robustness on ten wings, not every diagnostic.
6. **Stability verdicts are not available.** Every design would be unstable with its CG at 0.40 m,
   but 0.40 m is a convention, not a mass model. Neutral points (0.24–0.39 m) are trustworthy;
   verdicts need per-design CGs.
7. **Several thresholds are ours, not published.** They are reasoned and measured, but a reviewer
   may disagree:
   - the 1-count decision gate;
   - the friction band 0.6–1.6;
   - the 0.2° twist tolerance;
   - the 0.15 % leading-edge acceptance.
8. **The tools that measure have repeatedly been the source of error.** Examples:
   - defects 14–25;
   - on 15 September alone, five measurements (slicing, shock matching, index-picked row,
     twist corner, pressure-drag-only reading);
   - two confounded tests (tip cap inside the TE and far-field levels; layers inside the far-field
     level).

   All were caught by cross-checks, but the pattern says new analysis tools deserve an
   independent check before their numbers drive a decision. The rule now in the project's
   working notes: every run must differ from its baseline in exactly one setting, and must
   answer a question whose answer changes a decision.
9. **Authorizations were given in conversation** and written into policy by the assistant. This
   is recorded openly in each policy, but it is not a signature.
10. **The cloud cost estimate is unreliable** until a rented host is timed (the probe stage in
    `cloud_run.sh` exists for that).

### 8.4 What went well — and should be kept

- **Measure, don't assume.** The stopping rule, memory laws, rank scaling, directional
  attribution and the tip cap were each settled by a run designed for the question.
- **Retractions are recorded where the claim was made** (defect 23, the twist audit, the M6
  numbers), not quietly overwritten.
- **Guards were added at the moment each failure was understood**, so each class of failure can
  only happen once.
- **The run list is challenged against decisions**, which on 15 September cut about 17 hours of
  runs that could not change anything.

---

## Appendix A — glossary

| Term | Meaning |
|---|---|
| ADflow | University of Michigan MDO Lab's structured finite-volume RANS solver (MACH-Aero framework) |
| ANK / NK | Approximate Newton–Krylov / Newton–Krylov: implicit nonlinear solvers; ANK is robust early, NK converges fast late but can freeze |
| AVL | Athena Vortex Lattice: fast inviscid lifting-surface method |
| BWB | blended-wing-body, a tailless flying wing |
| CD, CDp, CDv | drag coefficient; its pressure and viscous (friction) parts |
| CGNS | CFD General Notation System, the mesh/solution file format |
| CL, CMy | lift and pitching-moment coefficients |
| Count | 0.0001 of CD |
| χ (chi) | SA freestream ratio ν̃/ν; χ = 3 is NASA TMR's prescription |
| Dissipation (artificial) | numerical smoothing that stabilises central schemes; scalar or matrix form; `vis2`, `vis4` |
| DPW / HiLiftPW | AIAA Drag / High-Lift Prediction Workshops |
| Far field | the outer boundary of the computational domain |
| GCI | Grid Convergence Index, a numerical uncertainty band from three grid levels |
| `gci_C/M/F/FF` | S8's coarse / medium / fine / extra-fine grid levels |
| Hold-out | a geometry set locked until the method is frozen, so results cannot be tuned to it |
| liftIndex | which axis ADflow treats as "up" |
| Loft | a smooth surface through section curves (pyGeo) |
| MAC | mean aerodynamic chord |
| Maximin | choosing points that maximise the minimum distance between them |
| Neutral point | the CG position at which pitch stability is neutral |
| O-H topology | O-shaped rings around each section, stacked in an H arrangement along the span |
| Observed order p | how fast error shrinks with refinement, measured from three grids |
| Path noise | difference between two converged runs that took different solver paths |
| RANS | Reynolds-Averaged Navier–Stokes |
| r | refinement ratio between grid levels (1.3) |
| Residual | imbalance of the discrete equations; falls as the solver converges |
| s0 | first cell height at the wall |
| SA | Spalart–Allmaras one-equation turbulence model (SA-Edwards is a variant) |
| SST | Menter's k-ω SST two-equation turbulence model |
| SU2 | Stanford's open-source CFD code, the second solver |
| TMR | NASA Turbulence Modeling Resource: verification/validation cases with agreed answers |
| Transition | change from laminar to turbulent boundary layer; not modelled in ADflow |
| V&V | verification (solving right) and validation (right equations) |
| WSL | Windows Subsystem for Linux, where the desktop runs |
| y+ | dimensionless wall distance of the first cell; ≤ 1 for wall-resolved RANS |

---

## Appendix B — the defect ledger

| # | Date | Defect | Consequence | Now |
|---|---|---|---|---|
| 1–11 | 4 Sept | construction defects in the first build | three looked like folded cells and were not | fixed |
| 14 | 4 Sept | `liftIndex` omitted: α applied as sideslip | every early result void | fixed; read back before every solve |
| 15 | 4 Sept | leading-edge spacing jumped 2.6× between stations | invisible to the turning metric | minimum envelope, ≤ 1.15× |
| 16 | 4 Sept | convergence readback read a non-existent attribute | a converged run recorded as failed | fixed |
| 17 | 5 Sept | two sign errors in a stability comparison (assistant's) | a false CFD–AVL disagreement | corrected; integrate the field when in doubt |
| 18 | 5 Sept | the mesher built an entire S6 surface to read the loft | died on design 0 | fixed |
| 19 | 5 Sept | fold check in-plane per section | folded meshes written, exit 0 | 3D hex check; raises |
| 20 | 5 Sept | SVD-fitted marching plane tilted up to 8° | 15 % of designs folded | span-normal frame |
| 21 | 5 Sept | span-normal fix projected the wall | wing moved 1.2 mm, all quality metrics unchanged | offset restored; 1e-9 m guard |
| 22 | 6 Sept | AVL moment arm applied twice | wrong AVL moments on a plot | fixed |
| 23 | 10 Sept | every pilot wing normalised by design 83's area | nine wings' coefficients off −25…+31 %; a published conclusion retracted | own areas; refusals |
| 24 | 13 Sept | geometry label read from directory name | a correct run archived as the wrong wing | read from grid path |
| 25 | 13 Sept | missing gate verdict passed the archive filter | an unjudged run in the archive | excluded |
| — | 13 Sept | tip cap at y+ 2.9–4.5 | archive fails the mandatory y+ rule | cap fixed; re-run approved |
| — | 15 Sept | M6 slicer: nearest row + tip-cap cells | pressure RMS doubled; fake suction spike | interpolated at station; cap zones skipped |
| — | 15 Sept | shock detector matched different lambda legs | a fake 0.34 x/c error | nearest-rise matching |
| — | 15 Sept | guideline check on an index-picked ring | a verdict flipped with span clustering | worst of every span row |
| — | 15 Sept | twist measured at a trailing-edge corner on the tip closure | false 0.19–0.76° washout deficit | trailing-edge midpoint, generator stations |
| — | 15 Sept | TE and far-field test levels carried the new tip cap | two confounded results | recomputed; clean tests queued |

---

## Appendix C — file map

**Start here:** this report; then `STATE_2026-09-10.md` for how to operate.

| What | Where (under `AERIS_MESH_STUDY/`) |
|---|---|
| This report, audits, plan, state | `04_strategy_studies/S8_oh_structured/*.md` |
| Mesher | `strategy_s8.py` (levels), `build_volume.py`, `march_o.py`, `section_o.py`, `write_cgns.py`, `m6_loft.py` |
| Solver drivers | `solve_s8.py` (wings), `onera_m6.py`, `naca0012_tmr.py`, `naca4412_tmr.py`, `su2_mesh.py`, `su2_config.py`, `su2_naca0012.py` |
| Judgement | `convergence_gate.py`, `equation_convergence.py`, `audit_runs.py`, `mesh_guidelines.py`, `geometry_audit.py`, `compare_meshes.py`, `gci.py` |
| Data | `dataset_row.py`, `collect_dataset.py` → `05_s6_cfd_qualification/dataset/` |
| Aerodynamic analyses | `cg_limits.py`, `surrogate_check.py`, `transition_sensitivity.py`, `chi_offset.py`, `verify_against_avl.py`, `m6_loads.py` |
| Orchestration | `run_campaign.py`, `select_levels.py`, `cloud_batch.py`, `cloud_preflight.py`, `cloud_run.sh`, `artifacts/s8_checklist/queue*.sh` (queue scripts are committed with `git add -f`) |
| Reports (evidence) | `05_s6_cfd_qualification/reports/s8_*.json` |
| Acceptance criteria | `05_s6_cfd_qualification/VV_ACCEPTANCE_2026-09-13.md` |
| Policies | `05_s6_cfd_qualification/policies/s8_campaign_v1.yaml`, `s8_cloud_hf_v1.yaml` |
| Decisions | `00_governance/decisions/ADR-0018`, `ADR-0019`, `ADR-0020` |
| External reference data | `05_s6_cfd_qualification/external/` (TMR NACA 0012, NACA 4412; ONERA M6) |
| Figures | `05_s6_cfd_qualification/viz/s8_*.png` |
| Scratch outputs (not in git, wiped between campaigns) | `artifacts/` |

---

## Appendix D — directions that shaped the work

The principal investigator's own words (typos kept), in order:

| Date | Direction | What it changed |
|---|---|---|
| 3 Sept | "we must be able to produce the paper within this month" | pace |
| 4 Sept | "i want to debug why the cfd doesnt converge... propably the mesh. so we shoudld change our meshing approach" | S8 |
| 4 Sept | "i want to think like an anegineer and make it work with y+<1" | wall-resolved from the start |
| 5 Sept | "implement the PLAN_desktop_campaign.md" | the campaign |
| 6 Sept | "i thought we decided that 10^-6 is enough to accept... update the policy and follow it throughout every cfd from now on" | stopping rule 1e-6 |
| 6 Sept | (the NASA ONERA M6 archive link) "make sure you set uo correct the M6 wing verification study" | M6 validation |
| 6 Sept | "verification/validation first, then generalization, then GCI" (plan order) | sequencing |
| 6 Sept | "save all the cfd results that we trust in the same folder with info... for reproducability" | the archive |
| 9 Sept | "maybe change the solver to su2?" | SU2 as second solver |
| 9 Sept | "i want tk make sure we dod everything, every setting checked before trying the cloud part" | settings on trial |
| 10 Sept | "make a full audit on the studies... can we trust our resluts so far?" | AUDIT_2026-09-10, defect 23 |
| 11 Sept | "challenge each one of them, even the settings like the free stream tirnulence standard" | χ 3 |
| 12 Sept | "lets follow fluent process... check that every passed run has all the variables indeed converged" | per-equation convergence |
| 12 Sept | "we make the runs and we comapre the cp's in all the stations, we compare our runs agaisnt the experimental data, nothing more" | M6 judged on measurement only |
| 12 Sept | "ok transition to NASA default incoming-air setting" | χ 3 adopted |
| 13 Sept | "the wings vary in terms of dihedral twist etc? the solvera and solutions are physically correct?" | geometry audit |
| 13 Sept | "nothing is sqare. the tip is airfoil shaped" / "we should mandatory achieve <1" | wall-resolved tip cap |
| 13 Sept | "we should validate our runs against SU2 solver" | SU2 route |
| 13 Sept | "exhaustive search... TMR NASA and AGARD... acceptance criteria" and "make sure these data refer to similar cases" | VV_ACCEPTANCE, NACA 4412 |
| 15 Sept | "ok approve the re-run with the new tip cap" | re-run approval |
| 15 Sept | "why we qued all these runs? what is the purpose. i want you to challenge the run list" → "i agree with you" | run list cut; settings first |
| 15 Sept | "i want everything from the start of the S8 to now to be defined and explained" | this report |

---

## Addendum — 16 September, 00:30: the settings tests of queue20

Each test is one change against a run with the same tip cap (design 83, α 0 unless stated).

| Test | Coarse `gci_C` | Medium `gci_M` | Reading |
|---|---|---|---|
| vis4 halved | −20.6 counts | **−12.0** | the dissipation effect shrinks with refinement by 0.58 per level, about second order: ordinary discretization error that the grid family removes. **Keep ADflow's default dissipation**, which is robust |
| Matrix dissipation | did not converge: ANK held CFL at 42–88; with NK allowed the density residual grew 27× over 400 iterations and NK never engaged | skipped | **not a candidate setting** |
| Trailing edge at 0.1 % of chord | ≈ −10.5 (tip-cap share removed) | **−3.8** | shrinks faster than dissipation (0.36 per level): a coarse-mesh error. But it costs no cells and moves the coarse level ~10 counts toward the converged answer. **Recommended: adopt at every level** (a DPW guideline) |
| Far field 100 chords, gci_C's own 65 layers | **+1.5** at α 0 and at α 8 | — | agrees with the 1/R estimate from the 40 → 60 pair (~+1.3). Half is friction: the same 65 layers now march 100 chords and viscous-layer growth rose to 1.273. Keeping 40 chords carries a known ~1.5-count offset, inside the workshops' 4–5 count scatter; 100 chords with growth under 1.25 needs ~30 more layers, and extra layers add drag at fixed chordwise resolution (C8) |

With vis4 halved the medium mesh reads **167.8 counts** at α 0, the value the chordwise three-level
extrapolation predicted (167.8). An indication, not a GCI, that design 83's converged drag at α 0 is
around 165–170 counts, some 40 below the coarse level.

**Decisions for the principal investigator:** dissipation — keep the default (recommended); trailing
edge — adopt 0.1 % at every level (recommended); far field — keep 40 chords for the desktop levels
with the offset stated, or 100 chords for the cloud levels if the budget allows.

Still running: NACA 4412 o1024 with a stronger NK; SU2 numerics variants on NACA 0012; then SU2
validation stage 0 (`queue21.sh`, `su2_validation.py`).
