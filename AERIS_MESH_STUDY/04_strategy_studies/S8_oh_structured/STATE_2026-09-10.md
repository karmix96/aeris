# S8 campaign state — resume here

**Written 2026-09-10.** Supersedes `HANDOFF_gci_M.md`, which describes `gci_M` as the next
step; that work is long finished. The plan is `PLAN_desktop_campaign.md`, and its §0 is still
the thing to read before running anything.

A session restart on 2026-09-10 took the scratchpad and every log in it. **Keep job logs in
`AERIS_MESH_STUDY/artifacts/`**, which survives restarts (though it is gitignored and gets
wiped between campaigns — anything worth keeping goes in `05_s6_cfd_qualification/`).

## Where the plan stands

| step | state |
|---|---|
| 1. freeze coarse strategy | done — method frozen, `gci_C` *resolution* explicitly not |
| 2. ONERA M6 validation | done — suction peak 1.0 %, shock position 0.015 x/c |
| 3. ten-geometry generalization | done — 40/40 `gci_C`, 12/12 `gci_M`, all accepted |
| 4. prepare HF batch | done — signed, preflighted, `cloud_run.sh` |
| **pre-cloud audit** | **in progress — see below** |
| 5. cloud HF run | not started |
| 6. GCI | needs step 5, or possibly not — see the chordwise finding |

## Findings that change decisions

**Pressure drag is carried by chordwise resolution.** `gci_C → gci_M` drops CDp by a constant
−0.00273 ± 6 % across three geometries and four incidences; CL, CDv, CMy and the neutral point
are converged. Refining one axis at a time recovers, of that gap:

| direction | cells | recovered |
|---|---|---|
| chord (`n_side` 45→58) | 744,828 | **88 %** |
| span (`n_span` 49→63) | 649,688 | 11 % |
| edges (LE/TE spacing) | 567,256 | 7 % |
| normal (`n_normal` 65→84, s0 held) | 733,316 | **−15 %** |

A chord-only level at `gci_F`'s chordwise ratio is ~975k cells / ~10 GiB and **fits this
desktop**, where `gci_F` (2.2M) does not. Whether chord-only *keeps* converging is being tested
now. Evidence: `reports/s8_directional_refinement.json`.

**The AVL multifidelity correction is geometry-specific.** CFD−AVL CL varies 3.5× more across
geometries than across incidence (stdev 0.033 vs 0.009); at α=8 it ranges −25 % to +32 %. Index
83 happens to agree to 2 %, which is why it looked fine. "AVL + a fitted correction" does not
work as a cheap surrogate as premised. PLAN §4.3 predicted exactly this test.

**The 1e-6 stopping rule holds** on three geometries unlike index 83: worst force movement
beyond it is 0.073 %, ~340× below the CDp signal. `reports/s8_l2_revalidation.json`.

**y+ is wall-resolved on every planform.** OML p99 0.68–0.83 across all ten. The y+ 3–5 in the
tables is the tip cap's 264 cells, by design.

**Meshing is deterministic.** 37/37 `compare_meshes` metrics identical across two independent
builds of index 83.

## Running and queued right now

All under `AERIS_MESH_STUDY/artifacts/`. Each job waits for its predecessor's final
`=== done` line, so they run one at a time (PLAN §0.2) without supervision.

| job | script | log | tests |
|---|---|---|---|
| running | `s8_aniso/chord2.sh` | `s8_aniso/chord2.out` | chord ×1.69, 974,800 cells, α 0 and 4 |
| after chord2 | `s8_aniso/chord_gci.sh` | `s8_aniso/chord_gci.out` | 3-level chordwise GCI (read-only) |
| after chord2 | `s8_aniso/s0_queue.sh` | `s8_aniso/s0_queue.out` | first-cell height alone |
| after s0 | `s8_ranks/ranks_queue.sh` | `s8_ranks/ranks_queue.out` | rank scaling at np 1, 2, 4, 6 |

Check them with `grep -E "CL=|done|FAILED" <log>`.

**The chordwise GCI uses `--ratios 1.2982 1.2955`, and that is not optional.**
`directional_level` scales *intervals*: 44 → 57 → 74 chordwise. `gci.py`'s default ratio,
`(N1/N2)^(1/3)`, assumes all three directions refine and returns ~1.09 for a one-direction
family — tripling the reported order and refusing a sound triplet.

## Still to do before renting

1. **Multigrid.** `MGCycle` is `"sg"` everywhere and has never been tried; it could cut cloud
   runtime substantially. Needs a `--mg-cycle` flag in `solve_s8.py`. **Add it only once every
   queued solve has finished** — the scripts invoke `solve_s8.py` afresh per run, and changing
   the solver between runs of one comparison invalidates it, however harmless the edit looks.
2. Read the chordwise GCI. If chord-only extrapolates to where global refinement is heading,
   converged drag is a desktop problem and the cloud batch can shrink or change shape.
3. Decide what the AVL finding means for the surrogate strategy before 100 designs are run.

## Traps specific to this host

- Interpreters: the plan's `/home/mike/...` paths are another machine. Use `env_s8.py`.
- ADflow writes **ADF**, not HDF5. h5py reads nothing and says nothing. Use `cgns_read.py`.
- `*.png` and `*.sh` are gitignored — both need `git add -f`.
- Work on `main`. No GitHub credentials here: hand `! git push` to the user.
- Pilot run directories are `<level>_a<alpha>`. They were once `a<alpha>` with no level, and a
  second grid level silently skipped every run and then **mislabelled** the first level's forces.
- `printf` with a `%` in a commit message truncates it silently. Use a heredoc.
